  frontend:
    build:
      context: .
      dockerfile: Dockerfile-Front
    image: angular:17.0.3
    container_name: frontend
    restart: always    
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    volumes:
      - frontend_data:/app/data
