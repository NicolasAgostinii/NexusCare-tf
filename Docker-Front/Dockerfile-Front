# Usa imagem leve com Node.js
FROM node:18-alpine

# Cria diretório de trabalho
WORKDIR /app

# Copia o conteúdo do projeto Angular para dentro do contêiner
COPY ./hackaton-front/nexusCareFront/ /app/

# Instala dependências do sistema necessárias para bibliotecas nativas
RUN apk add --no-cache git python3 make g++

# Instala dependências do Node.js
RUN npm install

# Expõe a porta padrão usada pelo Angular CLI
EXPOSE 4200

# Comando padrão ao iniciar o container
CMD ["npm", "start"]
